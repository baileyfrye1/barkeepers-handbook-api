using api.DTOs.Cocktails;
using api.Models;

namespace api.Interfaces
{
	public interface ICocktailRepository
	{
		// Task<List<CocktailDto>> GetAllAsync();
		// Task<Cocktail?> GetByIdAsync(int id);
		// Task<Cocktail> CreateAsync(Cocktail cocktailModel);
		// Task<Cocktail?> UpdateAsync(int id, UpdateCocktailRequestDto cocktailModel);
		// Task<Cocktail?> DeleteAsync(int id);
	}
}